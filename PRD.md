# PromptLib — 产品需求文档（PRD）

## 产品开发进展

### 功能迭代（2026-03-12 确认）

| 功能 | 说明 | 涉及模块 |
| :--- | :--- | :--- |
| 展开正文选中复制 | 展开 Prompt 正文后，用户可自由选中部分文字，通过右键菜单或快捷键（Ctrl+C / Cmd+C）复制选中内容到剪贴板；选中文字时点击不会触发卡片收起 | PromptCard |
| 产品 Logo 设计 | 三层正方形活页叠放图标：顶层深紫（`#5B21B6`）+ 底两层薰衣草紫（`#A78BFA`），顶层页面居中白色字母 P（Outfit 700），背景浅灰横线在字母区域断开留间隙，无底板透明背景，128x128 PNG | public/icons |

### 技术选型决策（2026-03-02 确认）

| 技术选型 | 方案 |
| :--- | :--- |
| 前端框架 | React |
| 构建工具 | Vite + @crxjs/vite-plugin |
| 样式方案 | CSS Modules |
| Markdown 渲染 | marked |
| 包管理器 | npm |
| 开发语言 | TypeScript |
| 数据存储 | chrome.storage.local |
| ID 生成 | crypto.randomUUID() |

### 原型设计决策（2026-03-02 确认）

| 决策点 | 方案 | 说明 |
| :--- | :--- | :--- |
| 功能覆盖范围 | 三个阶段全部功能 | P0 核心功能 + P1 检索与效率 + P2 数据安全 |
| 侧边栏宽度 | 360px | Chrome Side Panel 常见默认宽度 |
| 标签筛选布局 | 顶部多行自动换行 pill 标签栏 | 避免左右分栏在窄面板下空间不足，标签以 flex-wrap 多行展示，用户一眼可见全部标签 |
| 导入/导出入口 | 右上角「更多」菜单（⋯ 下拉） | 当前设置项仅导入/导出，单独设置页过重，下拉菜单最轻量 |
| 正文查看方式 | 手风琴式原地展开 | 点击列表项就地展开正文，再次点击收起，不离开列表页 |
| 变量填写表单 | 模态弹窗（Modal） | 居中浮层 + 背景遮罩，轻量临时操作，不离开列表上下文 |
| 产品名称字体 | 双色方案 + Outfit 字体 | "Prompt" `#5B21B6`（深紫）+ "Lib" `#A78BFA`（薰衣草紫），Outfit 700 weight 20px，科技感现代风格 |

### 需要呈现的页面/状态

1. 主列表页 — 搜索框 + 新建按钮 + 更多菜单 + 标签栏 + prompt 列表（含置顶区）
2. 列表项展开态 — 某条 prompt 展开显示 Markdown 正文
3. 列表项悬停态 — 显示编辑/复制/置顶/删除操作按钮
4. 创建/编辑页 — 标题 + 标签 + 正文表单
5. 变量填写弹窗 — 复制含变量 prompt 时的模态表单
6. 删除确认弹窗 — 二次确认对话框
7. 更多菜单展开 — 导入/导出选项
8. 导入结果提示 — 导入完成后的结果摘要

---

## 一、产品概述

| 项目 | 内容 |
| :--- | :--- |
| 产品名称 | PromptLib |
| 产品形式 | Chrome 浏览器插件（Side Panel 侧边栏） |
| 一句话描述 | 帮助 AI 大模型使用者在浏览器内实时管理和调用提示词模板 |
| 界面语言 | 中文（简体） |
| 目标平台 | Chrome 浏览器 |

## 二、目标用户

投入较多时间使用 AI 大模型（网页端）进行工作提效、开发、创作的专业或业余爱好人员。

**用户特征：**
- 日常通过浏览器使用 ChatGPT、Claude、Gemini 等 AI 大模型
- 对 prompt 质量有一定要求，会反复打磨和复用优质 prompt
- prompt 管理数量预计在 100 条以内

## 三、用户痛点与目标

### 痛点

| 场景 | 现状 | 问题 |
| :--- | :--- | :--- |
| 编写 prompt | 先在飞书/备忘录/Notion 等外部工具中打草稿，再复制到 AI 对话框 | 需要频繁切换窗口，打断工作流 |
| 收藏优质 prompt | 在文档工具中手动记录和分类 | 文档缺乏维护时内容混乱，查找耗时 |
| 复用 prompt | 从文档中搜索或翻找之前保存的 prompt | 没有统一管理入口，效率低 |

### 目标状态

通过浏览器侧边栏插件，用户可以在任意页面上：
- 快速创建、编辑、保存 prompt
- 通过标签分类和关键词搜索快速定位 prompt
- 一键复制 prompt 到剪贴板，支持变量替换
- 置顶常用 prompt，提升访问效率

## 四、功能需求

### 4.1 Prompt 展示模块（主列表）

**描述：** 打开侧边栏后展示用户所有已保存的 prompt 列表。

**展示规则：**
- 置顶的 prompt 优先展示在列表最上方
- 非置顶 prompt 按最后修改时间倒序排列
- 每条 prompt 显示：标题、所属标签、最后修改时间

**交互：**
- 鼠标悬停（hover）到某条 prompt 上时，显示操作按钮：编辑、复制、置顶/取消置顶、删除
- 点击某条 prompt 展开查看正文内容（Markdown 渲染）

### 4.2 创建 Prompt

**描述：** 用户点击「新建」按钮，进入 prompt 创建表单。

**表单字段：**

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 标题 | 单行文本输入 | 是 | 支持手动输入或粘贴 |
| 标签 | 单行文本输入 | 否 | 自定义场景分类标签，多个标签用英文逗号分隔（如 `vibe coding,开发`） |
| 正文 | 多行文本输入 | 是 | 支持 Markdown 格式，支持手动输入或粘贴 |

**正文编辑区附加说明：**
- 编辑区上方显示提示文字：`使用 {{变量名}} 插入变量`

**操作按钮：**
- 「保存」— 校验必填字段后保存 prompt，返回主列表
- 「取消」— 放弃当前编辑，返回主列表（若有未保存内容，弹出确认提示）

### 4.3 编辑 Prompt

**描述：** 对已保存的 prompt 进行修改。

- 进入编辑状态时，表单预填当前 prompt 的标题、标签、正文
- 编辑完成后点击「保存」更新内容，同时更新最后修改时间
- 点击「取消」放弃修改

### 4.4 复制 Prompt

**描述：** 将 prompt 正文复制到系统剪贴板。

**无变量的情况：**
- 点击复制按钮后，正文内容直接复制到剪贴板
- 显示"已复制"的短暂提示（toast）

**有变量的情况：**
- 正文中包含 `{{变量名}}` 格式的变量时，点击复制后弹出变量填写表单
- 表单列出所有变量名及对应输入框，变量名作为输入框的标签
- 用户填写完毕后点击「确认复制」，插件将变量替换为用户输入的值后复制到剪贴板
- 用户也可点击「跳过，直接复制」，保留 `{{变量名}}` 原样复制

### 4.5 置顶/取消置顶

**描述：** 将常用 prompt 固定在列表顶部。

- 点击置顶按钮后，该 prompt 移至列表顶部的置顶区域
- 已置顶的 prompt 显示「取消置顶」按钮
- 多条置顶 prompt 之间按置顶时间倒序排列

### 4.6 删除 Prompt

**描述：** 删除一条 prompt。

- 点击删除按钮后，弹出确认对话框：「确认删除这条 Prompt 吗？此操作不可恢复。」
- 确认后永久删除该 prompt

### 4.7 标签筛选

**描述：** 通过标签快速过滤 prompt 列表。

- 侧边栏左侧展示标签列表，标签从所有 prompt 中自动提取并去重
- 点击某个标签后，主列表仅显示包含该标签的 prompt
- 提供「全部」选项，点击后恢复展示所有 prompt
- 标签列表按字母/拼音排序

### 4.8 搜索

**描述：** 通过关键词搜索 prompt。

- 侧边栏顶部显示搜索输入框
- 搜索范围覆盖 prompt 的标题和正文内容
- 实时搜索（输入即筛选），无需点击搜索按钮
- 搜索结果高亮匹配的关键词

### 4.9 导入/导出

**描述：** 支持 prompt 数据的备份与恢复。

**导出：**
- 在设置/菜单中提供「导出数据」按钮
- 将所有 prompt 数据导出为 JSON 文件并触发浏览器下载
- 导出文件命名格式：`promptlib_backup_YYYYMMDD.json`

**导入：**
- 在设置/菜单中提供「导入数据」按钮
- 用户选择本地 JSON 文件后，插件校验文件格式
- 导入策略：若导入数据中的 prompt 与已有数据存在重复（以标题判断），提示用户选择「覆盖」或「跳过」
- 导入完成后显示结果摘要（新增 X 条，覆盖 X 条，跳过 X 条）

## 五、信息架构与页面结构

```
PromptLib 侧边栏
├── 顶部区域
│   ├── 搜索输入框
│   └── 「新建」按钮
├── 内容区域
│   ├── 左侧：标签筛选栏
│   │   ├── 「全部」选项
│   │   └── 标签列表（自动提取去重）
│   └── 右侧：Prompt 列表
│       ├── 置顶区域
│       └── 普通列表（按修改时间倒序）
├── Prompt 详情/编辑（覆盖主列表展示）
│   ├── 标题输入
│   ├── 标签输入
│   ├── 正文输入（Markdown）
│   └── 保存 / 取消 按钮
└── 底部/菜单
    ├── 导入数据
    └── 导出数据
```

## 六、UI/视觉设计规范

**整体风格：** 简洁极简（参考 Apple Notes、Notion 侧边栏）

**设计原则：**
- 大量留白，保持界面呼吸感
- 浅灰色背景（如 `#F5F5F7`），白色卡片/内容区域
- 使用细线分隔（`1px solid #E5E5E5`），无多余装饰
- 文字层级通过字号和字重区分，避免使用过多颜色
- 操作按钮采用图标形式，悬停时显示文字提示（tooltip）

**字体：**
- 使用系统默认字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`

**配色：**
| 用途 | 色值 |
| :--- | :--- |
| 主背景 | `#F5F5F7` |
| 卡片/内容区背景 | `#FFFFFF` |
| 主文字 | `#1D1D1F` |
| 次要文字 | `#86868B` |
| 分隔线 | `#E5E5E5` |
| 强调色/主操作 | `#007AFF` |
| 危险操作（删除） | `#FF3B30` |
| 成功提示 | `#34C759` |

## 七、数据模型

### Prompt 对象

```json
{
  "id": "string (UUID)",
  "title": "string",
  "tags": ["string"],
  "content": "string (Markdown)",
  "isPinned": false,
  "pinnedAt": null,
  "createdAt": "ISO 8601 时间戳",
  "updatedAt": "ISO 8601 时间戳"
}
```

### 存储方案

- 使用 `chrome.storage.local` 进行本地持久化存储
- 数据以数组形式存储在 `prompts` 键下
- 存储容量上限：`chrome.storage.local` 默认 5MB，100 条 prompt 远低于此限制
- 数据备份通过导入/导出功能（JSON 文件）实现

## 八、技术方案

| 项目 | 方案 |
| :--- | :--- |
| 插件类型 | Chrome Extension Manifest V3 |
| 展示形式 | Chrome Side Panel API |
| 前端框架 | 推荐 React（轻量、组件化、生态成熟） |
| 构建工具 | Vite 或 Webpack |
| 样式方案 | CSS Modules 或 Tailwind CSS |
| Markdown 渲染 | marked / markdown-it 等轻量库 |
| 数据存储 | chrome.storage.local |
| 唯一 ID 生成 | crypto.randomUUID() |

## 九、MVP 开发分期

### 第一阶段（核心功能）

| 优先级 | 功能 |
| :--- | :--- |
| P0 | Prompt 展示列表（主列表） |
| P0 | 创建 Prompt（标题 + 标签 + 正文） |
| P0 | 保存 / 取消 |
| P0 | 编辑 Prompt |
| P0 | 置顶 / 取消置顶 |
| P0 | 删除 Prompt |
| P0 | 复制到剪贴板（含变量替换） |

### 第二阶段（检索与效率）

| 优先级 | 功能 |
| :--- | :--- |
| P1 | 标签筛选 |
| P1 | 关键词搜索 |

### 第三阶段（数据安全）

| 优先级 | 功能 |
| :--- | :--- |
| P2 | 导出数据（JSON） |
| P2 | 导入数据（JSON） |

## 十、非功能性需求

| 类别 | 要求 |
| :--- | :--- |
| 性能 | 侧边栏打开时间 < 500ms；列表滚动流畅无卡顿 |
| 数据安全 | 所有数据存储在用户本地，不传输到任何外部服务器 |
| 容错 | 删除操作需二次确认；编辑未保存离开需提示 |
| 可维护性 | 代码结构清晰，组件化开发，便于后续迭代 |

## 十一、后续迭代方向（非 MVP 范围）

以下功能不在 MVP 范围内，作为后续版本的参考方向：

- 变量/占位符的可视化选中设置（方案 B）
- 独立标签管理（重命名、删除、合并、排序、标签颜色）
- 云端同步（需后端服务）
- 多语言支持（英文界面）
- 暗色主题
- Prompt 使用次数统计
- 直接插入到 AI 对话框（而非仅复制到剪贴板）
- 富文本编辑支持
- Prompt 分享功能（生成分享链接）
